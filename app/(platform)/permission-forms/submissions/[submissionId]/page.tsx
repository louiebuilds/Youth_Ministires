import{notFound,redirect}from"next/navigation";import{getAuthenticatedAccount}from"@/features/auth/services/session-service";import{getCustomFormSubmission}from"@/features/forms/services/custom-form-service";
function value(a:Awaited<ReturnType<typeof getCustomFormSubmission>>["answers"][number]){if(a.textValue!=null)return a.textValue;if(a.booleanValue!=null)return a.booleanValue?"Yes":"No";if(a.dateValue!=null)return a.dateValue;if(a.choiceValue!=null)return a.choiceValue;if(a.multipleChoiceValue?.length)return a.multipleChoiceValue.join(", ");return "No answer"}
export default async function CustomSubmissionPage({params}:{params:Promise<{submissionId:string}>}){if(!await getAuthenticatedAccount())redirect("/login");const data=await getCustomFormSubmission((await params).submissionId).catch(()=>null);if(!data)notFound();return <div className="space-y-5"><header><p className="text-sm font-semibold text-violet-700">Retained Custom Form</p><h1 className="text-3xl font-bold">{data.title} · v{data.versionNumber}</h1><p className="mt-2 capitalize text-slate-600">{data.assignmentType.replaceAll("_"," ")} · {data.status}</p></header>{data.answers.map(a=><article className="rounded-xl border bg-white p-5" key={a.fieldId}><h2 className="font-bold">{a.label}</h2><p className="mt-2 whitespace-pre-wrap text-slate-700">{value(a)}</p></article>)}</div>}
